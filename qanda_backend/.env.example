# Django settings
DJANGO_SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=.kavia.ai,localhost,127.0.0.1,testserver

# Database (defaults to SQLite if unset)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Timezone
TIME_ZONE=UTC

# OpenAI for /api/ask endpoint
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
